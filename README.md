# `hotfunc` (Hot Function): Load and execute a hot function in JavaScript/TypeScript

## How to use `hotfunc()`?

```js

// First, you need to import the `hotfunc` module
import hotfunc from 'hotfunc';

// Then, do whatever you want with `hotfunc`

```